[project]
name = "blog-quarto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "jupyter-cache>=1.0.0",
    "matplotlib>=3.10.3",
    "plotly>=6.1.2",
]
